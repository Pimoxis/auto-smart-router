interface:
  display_name: "Codex Smart Router"
  short_description: "Cost-conscious model routing for general tasks"
  default_prompt: "Use $codex-smart-router to complete my task, delegating worthwhile subtasks to suitable available models and escalating only when needed."
policy:
  allow_implicit_invocation: true
